{
  "type": "object",
  "properties": {
    "login-email": {
      "type": "string",
       "format": "email",
       "minLength": 6,
       "maxLength": 127
    },
    "login-password": {
      "type": "string",
      "pattern": "^([^0-9]*)$",
      "errorMessage": {
        "pattern": "Use only letters!"
     }
    }
  },
  "required": ["login-email", "login-password"]
}
