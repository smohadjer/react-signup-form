[
    {
        "id": "name",
        "label": "Name",
        "inputType": "text",
        "pattern": "\\w{3,30}",
        "placeholder": "Enter your first name",
        "error": "Name should contain only alphanumeric characters and be at least 3 characters and at most 30 characters long."
    },
    {
        "id": "email",
        "label": "Email",
        "inputType": "email",
        "placeholder": "Enter valid email address",
        "error": "The entry is not a valid email address.",
        "required": true
    },
    {
        "id": "password",
        "label": "Password",
        "inputType": "password",
        "placeholder": "Use a strong password",
        "pattern": "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[^a-zA-Z\\d])[^\\s]{8,}$",
        "error": "Password should have minimum 8 characters, at least one uppercase letter, one lowercase letter, one number and one special character.",
        "required": true
    }
]
